# Adicionar arquivos
    git add .

# Commitar arquivos - enviar mensagem entre as aspas
    git commit -m " "    

# PUSH envia do repositório local para o remoto (Github).
    git push -u origin main

# PULL envia do remoto (Github) para o repositorio remoto
    git pull 